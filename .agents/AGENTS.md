# Workspace Rules

- **Git Auto-Push**: Every time you modify files in the codebase, immediately run `git add`, `git commit -m "<message>"`, and `git push origin main` to ensure the remote GitHub repository is up-to-date.
